# huaweicloud-mate 建仓流程

## 完整流程

```
贡献者提交建仓表单（repository-requests → New Issue → 新建仓库申请）
    ↓
Issue 自动带 status/pending 标签
    ↓
Issue Bot 触发 → 分类标签 + 飞书私信审批卡片
    ↓
管理员在飞书点「✅ 审批通过」
    ↓
跳转 GitHub workflow 页面（Issue 号已预填）→ Run workflow
    ↓
自动打 request/create-repo 标签
    ↓
Repo Creator 触发
    ↓
├── 1. 创建仓库
├── 2. 分配角色（Maintainer / Writer / Triage）
├── 3. 注入社区文件
│   ├── CONTRIBUTING.md
│   ├── SECURITY.md
│   ├── .github/dependabot.yml
│   ├── .github/stale.yml
│   ├── .github/ISSUE_TEMPLATE/bug_report.yml
│   ├── .github/ISSUE_TEMPLATE/feature_request.yml
│   ├── .github/workflows/triage-issue.yml
│   ├── .github/workflows/sync-to-gitcode.yml
│   └── 14 个标准标签 + 安全告警
└── 4. 飞书提醒创建 GitCode 仓库
    ↓
Issue 关闭（status/completed）
    ↓
管理员在 GitCode 手动创建同名仓库
    ↓
每次 push → 自动同步到 gitcode.com/hd-vector/{仓库名}
```

## 建仓表单字段

### 基本信息（均必填）

| 字段 | 说明 |
|------|------|
| 仓库名称 | 小写字母、数字、连字符 |
| 仓库描述 | 简要说明用途 |
| 可见性 | public / private |
| 主要编程语言 | 8 种可选 |
| 开源许可证 | 5 种可选 |
| 申请理由 | 建仓目的 |

### 角色配置

| 字段 | 必填 | 权限 | 格式 |
|------|------|------|------|
| Maintainer（维护者） | ✅ | Issue/PR/设置管理，不可删库 | 逗号或换行分隔多个用户名 |
| Writer（写入者） | ✅ | 代码推送 | 同上 |
| Triage（分类者） | 可选 | Issue/PR 分类管理 | 同上 |

### 可选字段

| 字段 | 说明 |
|------|------|
| 仓库标签（Topics） | 逗号分隔 |

## 飞书消息

| 时机 | 类型 | 内容 |
|------|------|------|
| Issue 提交 | 审批卡片 | 仓库名 + 描述 + 语言 + 可见性 + 审批按钮 |
| 建仓完成 | 提醒卡片 | 仓库链接 + 角色分配 + GitCode 建仓按钮 |

## 组织 Secrets

| Secret | 用途 |
|--------|------|
| `GITCODE_USERNAME` | GitCode 同步认证 |
| `GITCODE_TOKEN` | GitCode 同步认证 |
| `FEISHU_APP_ID` | 飞书审批通知 + GitCode 提醒 |
| `FEISHU_APP_SECRET` | 飞书审批通知 + GitCode 提醒 |
| `FEISHU_ADMIN_OPEN_ID` | 飞书消息接收人 |

## 关键文件

| 文件 | 仓库 | 作用 |
|------|------|------|
| `ISSUE_TEMPLATE/create-repo.yml` | repository-requests | 建仓表单（含角色字段） |
| `workflows/repo-creator.yml` | repository-requests | 建仓触发器 |
| `scripts/repo_creator.py` | repository-requests | 建仓 + 角色分配 + GitCode 提醒 |
| `workflows/issue-bot.yml` | repository-requests | Issue 分类 + 飞书审批通知 |
| `workflows/approve-repo.yml` | repository-requests | 审批打标签工作流 |
| `actions/issue-bot/issue_bot.py` | .github | Issue Bot 脚本（含飞书通知） |
| `workflows/sync-to-gitcode.yml` | .github | 复用同步工作流 |

## GitCode 同步

同步工作流已注入每个新建仓库。需先在 https://gitcode.com/hd-vector 手动创建同名仓库，之后任何 push 自动同步。

## 管理员操作

```powershell
# 查看待处理建仓
gh issue list -R huaweicloud-mate/repository-requests -l "status/pending"

# 审批建仓
gh issue edit <编号> -R huaweicloud-mate/repository-requests --add-label "request/create-repo"

# 清理仓库
gh repo delete huaweicloud-mate/<name> --yes
```
